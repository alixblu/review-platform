# Production Environment Variables for EC2
# Copy this to .env and update with your EC2 public IP or domain

# Replace with your EC2 public IP or domain
EC2_PUBLIC_IP=your-ec2-ip-or-domain

# API Gateway URL (accessible from browser)
VITE_API_URL=http://${EC2_PUBLIC_IP}:8888

# Auth Service URL (for OAuth redirects)
AUTH_SERVICE_URL=http://${EC2_PUBLIC_IP}:8080

# Frontend URL (for OAuth redirects)
VITE_APP_URL=http://${EC2_PUBLIC_IP}:3000
FRONTEND_URL=http://${EC2_PUBLIC_IP}:3000

# Cognito Configuration (REQUIRED!)
VITE_COGNITO_DOMAIN=https://us-east-1jhi5wpbs9.auth.us-east-1.amazoncognito.com
VITE_COGNITO_CLIENT_ID=62te3nhct6ltri9o1acf44n5b5
COGNITO_CLIENT_SECRET=your-cognito-client-secret-here

# AWS Credentials for S3 Image Uploads (REQUIRED!)
AWS_ACCESS_KEY_ID=your-aws-access-key-here
AWS_SECRET_ACCESS_KEY=your-aws-secret-key-here

# AWS Credentials for Knowledge Base S3 (REQUIRED!)
AWS_ACCESS_KEY_ID_PROD=your-kb-aws-access-key-here
AWS_SECRET_ACCESS_KEY_PROD=your-kb-aws-secret-key-here
